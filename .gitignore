# Dependencies
node_modules/

# Build artifacts (generated by build-firefox.js / build-chrome.js)
build/
*.zip
*.xpi

# Backups and temp files
*.orig.bak
*.bak

# Local eval-gate dev script + fixtures — real captured chats and a machine-local
# path, kept out of the repo
tests/eval-gate.js
tests/_gate-samples/

# Personal / internal — kept local, not published
STATUS.md
make_store_shots.ps1
pack-xpi.js
mcp/
website/

# Generated store artwork — 55 PNGs regenerated on demand with
# `node tools/store-shots/render.js`, so there is no reason to version them.
# The copy they are built from IS tracked, under i18n/.
store-listings/

# Internal localization notes (keyword strategy, store-upload steps). The README
# covers what users need; this is working detail, kept local.
LOCALIZATION.md

# Tooling / editor / OS
.claude/
.agents/
skills-lock.json
.vscode/
.DS_Store
Thumbs.db

# Obsidian vault clutter — this repo folder doubles as an Obsidian vault locally.
# None of this is part of the extension (builds bundle only src/ + icons/); keep
# it out of git and out of the published builds.
.obsidian/
*.canvas
images/
20[0-9][0-9]-[0-9][0-9]-[0-9][0-9].md
